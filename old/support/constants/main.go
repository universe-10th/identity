package constants

const (
    Namespace  string = "universe-10th/identity"
    ID         string = Namespace + ":ID"
    Realm      string = Namespace + ":Realm"
    Credential string = Namespace + ":Credential"
)
