- Implement dummy hasher(s), dummy credential(s) and dummy source(s).
  - Done: dummy hasher, dummy credentials (with several traits).
- Test EVERYTHING (this one will be quite large) by creating samples/.
- Move the default implementations of hashers and sources to other (new packages).
- Update the README.md file.